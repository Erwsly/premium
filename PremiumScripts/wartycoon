local player = game.Players.LocalPlayer
local Player = game:GetService("Players")
local LocalPlayer = Player.LocalPlayer
local StarterGui = game:GetService("StarterGui")
local PlayerName = LocalPlayer.Name
local TycoonName
local cango

StarterGui:SetCore("SendNotification", {
    Title = "Credits",
    Text = "This script made by erwsly",
    Duration = 10
})

StarterGui:SetCore("SendNotification", {
    Title = "Information",
    Text = "Open/Close is binded to CTRL",
    Duration = 10
})

for _, tycoon in pairs(game:GetService("Workspace").Tycoon.Tycoons:GetChildren()) do
    if tycoon:FindFirstChild("Owner") and tostring(tycoon.Owner.Value) == PlayerName then
        TycoonName = tycoon.Name
        StarterGui:SetCore("SendNotification", {
            Title = "Tycoon Found",
            Text = tycoon.Name,
            Duration = 5
        })
        break
    end
end

-- Check if the player has a tycoon
local PlayerTycoon = game:GetService("Workspace").Tycoon.Tycoons[TycoonName]
if not PlayerTycoon then
        StarterGui:SetCore("SendNotification", {
            Title = "Tycoon Not Found",
            Text = "Did you choose it?",
            Duration = 5
        })
    awakenNotification:CreateNotification(
        'Error:',
        "Failed to find player tycoon. Make sure you're loaded in and have a tycoon!",
        14404156927,
        4
    )
    return
end

local collectorPosition = PlayerTycoon.Essentials["Oil Collector"].Collector.BarrelSlot.CFrame.Position
local success, part = pcall(function()
    return PlayerTycoon.Essentials["Oil Collector"].Collector.BarrelSlot
end)

if success and part then
    collectorPosition = part.CFrame.Position
else
    warn("Oil Collector part not found.")
end

-- Fonksiyon: Belirtilen CFrame'e oyuncuyu ışınlar
local function teleportPlayerToCFrame(cframe)
    player.Character:SetPrimaryPartCFrame(cframe)
end

-- ScreenGui oluştur
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TeleportGui"
screenGui.Parent = game.Players.LocalPlayer.PlayerGui

-- Ana Frame oluştur
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0.3, 0, 0.5, 0)
mainFrame.Position = UDim2.new(0.35, 0, 0.2, 0)
mainFrame.BackgroundColor3 = Color3.new(0.1, 0.1, 0.1)
mainFrame.BackgroundTransparency = 0.2
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

-- Köşeleri yumuşatmak için UI Corner ekle
local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 10)
uiCorner.Parent = mainFrame

-- Sürüklenebilir hale getirmek için fonksiyonlar
local dragging
local dragInput
local dragStart
local startPos

local function update(input)
    local delta = input.Position - dragStart
    mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

mainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

mainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

game:GetService("UserInputService").InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

-- Kapatma butonu oluştur
local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 50, 0, 50)
closeButton.Position = UDim2.new(1, -55, 0, 5)
closeButton.BackgroundColor3 = Color3.new(0.1, 0.1, 0.1)
closeButton.Text = "X"
closeButton.TextSize = 25
closeButton.TextColor3 = Color3.new(1, 1, 1)
closeButton.Parent = mainFrame

closeButton.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

-- Başlık oluştur
local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 0, 30)
titleLabel.Position = UDim2.new(0, 0, 0, 10)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "War Tycoon - Lemonade HUB"
titleLabel.Font = Enum.Font.SourceSansBold
titleLabel.TextSize = 18
titleLabel.TextColor3 = Color3.new(1, 1, 1)
titleLabel.Parent = mainFrame

-- ScrollingFrame oluştur
local scrollingFrame = Instance.new("ScrollingFrame")
scrollingFrame.Size = UDim2.new(1, -10, 1, -60)
scrollingFrame.Position = UDim2.new(0, 5, 0, 55)
scrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 55 * 22)  -- Kaydırılabilir alanı ayarla (butonların toplam yüksekliği)
scrollingFrame.ScrollBarThickness = 10
scrollingFrame.BackgroundTransparency = 1
scrollingFrame.Parent = mainFrame

local OilCollectorB = Instance.new("TextButton")
OilCollectorB.Size = UDim2.new(1, -10, 0, 45)
OilCollectorB.Position = UDim2.new(0, 5, 0, 0)
OilCollectorB.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
OilCollectorB.TextColor3 = Color3.new(1, 1, 1)
OilCollectorB.Text = "Oil Collector"
OilCollectorB.Parent = scrollingFrame

OilCollectorB.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(collectorPosition))
end)

local OilBarrel1 = Instance.new("TextButton")
OilBarrel1.Size = UDim2.new(1, -10, 0, 45)
OilBarrel1.Position = UDim2.new(0, 5, 0, 55)
OilBarrel1.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
OilBarrel1.TextColor3 = Color3.new(1, 1, 1)
OilBarrel1.Text = "Oil Barrel 1"
OilBarrel1.Parent = scrollingFrame

OilBarrel1.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(69.41250610351562, 105.25658416748047, 3436.249267578125))
end)

local OilBarrel2 = Instance.new("TextButton")
OilBarrel2.Size = UDim2.new(1, -10, 0, 45)
OilBarrel2.Position = UDim2.new(0, 5, 0, 110)
OilBarrel2.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
OilBarrel2.TextColor3 = Color3.new(1, 1, 1)
OilBarrel2.Text = "Oil Barrel 2"
OilBarrel2.Parent = scrollingFrame

OilBarrel2.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(78.37213134765625, 105.25657653808594, -2062.473876953125))
end)

local OilBarrel3 = Instance.new("TextButton")
OilBarrel3.Size = UDim2.new(1, -10, 0, 45)
OilBarrel3.Position = UDim2.new(0, 5, 0, 165)
OilBarrel3.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
OilBarrel3.TextColor3 = Color3.new(1, 1, 1)
OilBarrel3.Text = "Oil Barrel 3"
OilBarrel3.Parent = scrollingFrame

OilBarrel3.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(-27.435659408569336, 49.34040069580078, -418.55621337890625))
end)

local OilBarrel4 = Instance.new("TextButton")
OilBarrel4.Size = UDim2.new(1, -10, 0, 45)
OilBarrel4.Position = UDim2.new(0, 5, 0, 220)
OilBarrel4.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
OilBarrel4.TextColor3 = Color3.new(1, 1, 1)
OilBarrel4.Text = "Oil Barrel 4"
OilBarrel4.Parent = scrollingFrame

OilBarrel4.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(-14.359360694885254, 49.34040069580078, 700.2596435546875))
end)

-- Base butonları oluştur
local Foxtrot = Instance.new("TextButton")
Foxtrot.Size = UDim2.new(1, -10, 0, 50)
Foxtrot.Position = UDim2.new(0, 5, 0, 275)
Foxtrot.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Foxtrot.TextColor3 = Color3.new(1, 1, 1)
Foxtrot.Text = "Foxtrot"
Foxtrot.Parent = scrollingFrame

Foxtrot.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(997.367432, 48.420288, -2137.732666))
end)

local Golf = Instance.new("TextButton")
Golf.Size = UDim2.new(1, -10, 0, 50)
Golf.Position = UDim2.new(0, 5, 0, 330)
Golf.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Golf.TextColor3 = Color3.new(1, 1, 1)
Golf.Text = "Golf"
Golf.Parent = scrollingFrame

Golf.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(1922.708862, 48.410877, -1642.480469))
end)

local Echo = Instance.new("TextButton")
Echo.Size = UDim2.new(1, -10, 0, 50)
Echo.Position = UDim2.new(0, 5, 0, 385)
Echo.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Echo.TextColor3 = Color3.new(1, 1, 1)
Echo.Text = "Echo"
Echo.Parent = scrollingFrame

Echo.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(-947.773010, 48.420288, -2499.127686))
end)

local Delta = Instance.new("TextButton")
Delta.Size = UDim2.new(1, -10, 0, 50)
Delta.Position = UDim2.new(0, 5, 0, 440)
Delta.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Delta.TextColor3 = Color3.new(1, 1, 1)
Delta.Text = "Delta"
Delta.Parent = scrollingFrame

Delta.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(-1871.134033, 48.420288, -2070.459961))
end)

local Charlie = Instance.new("TextButton")
Charlie.Size = UDim2.new(1, -10, 0, 50)
Charlie.Position = UDim2.new(0, 5, 0, 495)
Charlie.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Charlie.TextColor3 = Color3.new(1, 1, 1)
Charlie.Text = "Charlie"
Charlie.Parent = scrollingFrame

Charlie.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(-2436.581299, 48.420288, -1376.598999))
end)

local Bravo = Instance.new("TextButton")
Bravo.Size = UDim2.new(1, -10, 0, 50)
Bravo.Position = UDim2.new(0, 5, 0, 550)
Bravo.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Bravo.TextColor3 = Color3.new(1, 1, 1)
Bravo.Text = "Bravo"
Bravo.Parent = scrollingFrame

Bravo.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(-2787.298096, 48.420284, -527.011658))
end)

-- Alpha butonunu oluştur
local Alpha = Instance.new("TextButton")
Alpha.Size = UDim2.new(1, -10, 0, 50)
Alpha.Position = UDim2.new(0, 5, 0, 605)
Alpha.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Alpha.TextColor3 = Color3.new(1, 1, 1)
Alpha.Text = "Alpha"
Alpha.Parent = scrollingFrame

Alpha.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(-2760.782227, 48.420288, 562.888794))
end)

-- Zulu butonunu oluştur
local Zulu = Instance.new("TextButton")
Zulu.Size = UDim2.new(1, -10, 0, 50)
Zulu.Position = UDim2.new(0, 5, 0, 660)
Zulu.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Zulu.TextColor3 = Color3.new(1, 1, 1)
Zulu.Text = "Zulu"
Zulu.Parent = scrollingFrame

Zulu.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(-2348.640137, 48.420284, 1593.650024))
end)

-- Victor butonunu oluştur
local Victor = Instance.new("TextButton")
Victor.Size = UDim2.new(1, -10, 0, 50)
Victor.Position = UDim2.new(0, 5, 0, 715)
Victor.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Victor.TextColor3 = Color3.new(1, 1, 1)
Victor.Text = "Victor"
Victor.Parent = scrollingFrame

Victor.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(-2384.778076, 48.420288, 2562.118896))
end)

-- Hotel butonunu oluştur
local Hotel = Instance.new("TextButton")
Hotel.Size = UDim2.new(1, -10, 0, 50)
Hotel.Position = UDim2.new(0, 5, 0, 770)
Hotel.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Hotel.TextColor3 = Color3.new(1, 1, 1)
Hotel.Text = "Hotel"
Hotel.Parent = scrollingFrame

Hotel.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(2838.965576, 48.420296, -1334.691895))
end)

-- Kilo butonunu oluştur
local Kilo = Instance.new("TextButton")
Kilo.Size = UDim2.new(1, -10, 0, 50)
Kilo.Position = UDim2.new(0, 5, 0, 825)
Kilo.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Kilo.TextColor3 = Color3.new(1, 1, 1)
Kilo.Text = "Kilo"
Kilo.Parent = scrollingFrame

Kilo.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(3023.072510, 48.420288, -469.696838))
end)

-- Lima butonunu oluştur
local Lima = Instance.new("TextButton")
Lima.Size = UDim2.new(1, -10, 0, 50)
Lima.Position = UDim2.new(0, 5, 0, 880)
Lima.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Lima.TextColor3 = Color3.new(1, 1, 1)
Lima.Text = "Lima"
Lima.Parent = scrollingFrame

Lima.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(3033.761719, 48.420288, 612.195923))
end)

-- Romeo butonunu oluştur
local Romeo = Instance.new("TextButton")
Romeo.Size = UDim2.new(1, -10, 0, 50)
Romeo.Position = UDim2.new(0, 5, 0, 935)
Romeo.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Romeo.TextColor3 = Color3.new(1, 1, 1)
Romeo.Text = "Romeo"
Romeo.Parent = scrollingFrame

Romeo.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(2356.477539, 48.420288, 2372.262695))
end)

-- Omega butonunu oluştur
local Omega = Instance.new("TextButton")
Omega.Size = UDim2.new(1, -10, 0, 50)
Omega.Position = UDim2.new(0, 5, 0, 990)
Omega.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Omega.TextColor3 = Color3.new(1, 1, 1)
Omega.Text = "Omega"
Omega.Parent = scrollingFrame

Omega.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(2930.722900, 48.420288, 1514.969360))
end)

-- Sierra butonunu oluştur
local Sierra = Instance.new("TextButton")
Sierra.Size = UDim2.new(1, -10, 0, 50)
Sierra.Position = UDim2.new(0, 5, 0, 1045)
Sierra.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Sierra.TextColor3 = Color3.new(1, 1, 1)
Sierra.Text = "Sierra"
Sierra.Parent = scrollingFrame

Sierra.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(1399.456299, 48.420288, 2690.309814))
end)

-- Tango butonunu oluştur
local Tango = Instance.new("TextButton")
Tango.Size = UDim2.new(1, -10, 0, 50)
Tango.Position = UDim2.new(0, 5, 0, 1100)
Tango.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
Tango.TextColor3 = Color3.new(1, 1, 1)
Tango.Text = "Tango"
Tango.Parent = scrollingFrame

Tango.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(-876.607178, 48.420288, 2502.087158))
end)

local capturepointbutton = Instance.new("TextButton")
capturepointbutton.Size = UDim2.new(1, -10, 0, 45)
capturepointbutton.Position = UDim2.new(0, 5, 0, 1155)
capturepointbutton.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
capturepointbutton.TextColor3 = Color3.new(1, 1, 1)
capturepointbutton.Text = "Capture Point"
capturepointbutton.Parent = scrollingFrame

capturepointbutton.MouseButton1Click:Connect(function()
    teleportPlayerToCFrame(CFrame.new(67.3052292, 47.2024422, 186.691132, 0.0376003385, 0.820677936, -0.570152521, 0.365953565, 0.519611716, 0.772063196, 0.929873109, -0.237679213, -0.280792415))
end)

-- GUI'yi Ctrl ile açıp kapatma
local UserInputService = game:GetService("UserInputService")

local guiVisible = true

game.Players.LocalPlayer.CharacterAdded:Connect(function(character)
    local humanoid = character:WaitForChild("Humanoid")

    humanoid.Died:Connect(function()
    end)
end)

screenGui.ResetOnSpawn = false

screenGui.Enabled = true

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if input.KeyCode == Enum.KeyCode.LeftControl and not gameProcessed then
        guiVisible = not guiVisible
        screenGui.Enabled = guiVisible
    end
end)
